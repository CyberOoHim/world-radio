{
  "name": "world_radio",
  "private": true,
  "version": "1.0.0",
  "description": "Relaxing world radio player with thousands of global stations",
  "type": "module",
  "homepage": "https://CyberOoHim.github.io/world_radio/",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy:check": "npm run build && node -e \"const fs=require('fs');const h=fs.readFileSync('dist/index.html','utf8');if(h.includes('/src/main.ts')||h.includes('src=\\\"/assets')){console.error('Bad asset paths in dist/index.html');process.exit(1)}console.log('dist OK');\""
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "@fontsource/dm-sans": "^5.2.8",
    "@fontsource/instrument-serif": "^5.2.8"
  }
}